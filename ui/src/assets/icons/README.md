# Icon set

Flat **2D SVG** icons (inline in `ui/src/icons/IconSet.tsx`). Transparent fills; strokes use `currentColor` so buttons control color.

Buttons are **outline-only**: green (`--accent`) by default, red (`--danger`) for destructive actions (`.button-danger`).
